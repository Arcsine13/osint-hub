services:
  - type: web
    name: sherlock-osint-backend
    runtime: image
    image:
      url: Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      - key: DB_PATH
        value: ./backend/data/osint-hub.db
